const middlewareError = require('./middlewareError');
const middlewareAuth = require('./middlewareAuth.js');
const middlewareBody = require('./middlewareBody');

module.exports = {
  middlewareError,
  middlewareAuth,
  middlewareBody,
};
